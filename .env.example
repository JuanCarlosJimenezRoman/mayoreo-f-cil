# Copiá este archivo como ".env" y completá con tus datos reales.
# Los obtenés al crear tu app en el panel de socios de Tiendanube:
# https://www.tiendanube.com/partners

CLIENT_ID=tu_client_id
CLIENT_SECRET=tu_client_secret

# URL pública donde vas a alojar este servidor (sin barra al final).
# Ej: https://mayoreo-baskatball23.up.railway.app
APP_URL=https://tu-servidor-publico.com

# Identifica tu app ante Tiendanube en cada request (obligatorio)
APP_USER_AGENT=Mayoreo BASKATBALL23 (tu-email@ejemplo.com)

PORT=3000

# Connection string de tu base PostgreSQL en Render
# (Internal Database URL si el server vive en el mismo proyecto de Render,
# External Database URL si te conectás desde otro lado)
DATABASE_URL=postgresql://usuario:password@host/nombre_db

# Ya NO hace falta configurar usuario/contraseña para /admin: cada
# tienda recibe automáticamente un "magic link" único al instalar la
# app (ver src/auth.js). Si necesitás el link de una tienda ya
# instalada, corré: node scripts/list-stores.js

# Usados en la landing page ("/") y en el mensaje de fin de prueba
SUPPORT_EMAIL=tu-email@ejemplo.com
PRICE_MONTHLY=$299 MXN

# Verificación de firma de webhooks (seguridad). Dejalo en "false" al
# principio para solo VER en los logs si todo firma bien, sin arriesgar
# cortar el mayoreo en vivo. Cuando confirmes que está todo OK en los
# logs por unos días, cambialo a "true" para que rechace de verdad
# las requests con firma inválida.
WEBHOOK_SIGNATURE_ENFORCE=false
